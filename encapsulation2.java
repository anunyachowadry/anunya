public class encapsulation2 {
        public static void main(String[] args){
            encapsulation obj=new encapsulation();
            obj.setName("Anu's Age is:");
            obj.setAge(22);                                //setting the name and age
            System.out.println(obj.getName());            //getting the name
            System.out.println(obj.getAge());              //getting the age

        }
    }

